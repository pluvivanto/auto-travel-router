services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: atr-backend
    ports:
      - "8080:8080"
    expose:
      - "8080"

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    container_name: atr-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
